import { NetworkError } from './NetworkError';
import { RestService, type RestServiceOptions } from './rest.service';

export { NetworkError, RestService, type RestServiceOptions };
export const { isNetworkError } = NetworkError;
