import { LocalStorageService } from './LocalStorageService';

export { LocalStorageKeys, LocalStorageService } from './LocalStorageService';
export const { useLocalStorageState } = LocalStorageService;
